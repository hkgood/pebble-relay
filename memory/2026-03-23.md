# 2026-03-23 工作日志

## ClawPop UI 改进

### 已完成修复

1. **双语支持**
   - Install 页面：添加 `t.install.progress` 翻译
   - Settings 页面：修复硬编码中文（停止、深色、浅色）
   - Config 页面：渠道名称根据语言动态显示
   - Success 页面：添加 `sendCmd`、`toStart` 翻译
   - LanguageSelector：添加「语言」/「Lang」翻译
   - Progress 组件：添加「进度」/「Progress」翻译

2. **亮暗模式适配**
   - 状态颜色：新增 CSS 变量 `--success-bg`、`--error-bg`、`--warning-bg`
   - Button 组件：统一使用 CSS 变量
   - ThemeToggle：简化样式，使用语义颜色
   - 页面标题：统一添加 `text-primary`
   - 移除不必要的 `animate-pulse` 动画

3. **设计一致性**
   - 移除硬编码颜色（`bg-brand-start` → `bg-brand`）
   - 统一使用语义颜色类
   - 首页按钮样式统一（设置和卸载都使用 ghost）
   - 滚动条位置统一

### 关键技术点

- 使用 `language` 变量动态切换多语言
- 亮色模式使用 `bg-hover`、`text-primary`、`text-secondary`
- 避免硬编码十六进制颜色，优先使用 CSS 变量
- 简化重复的条件判断（如 `theme === 'light' ? 'text-secondary' : 'text-secondary'`）
